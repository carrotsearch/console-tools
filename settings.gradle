dependencyResolutionManagement {
  versionCatalogs {
    libs {
      from(files('versions.toml'))
    }
  }
}

include 'launcher'
include 'formatters'
include 'testing'

rootProject.name = 'console-tools'
